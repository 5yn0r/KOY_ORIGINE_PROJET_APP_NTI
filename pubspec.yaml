name: myapp
description: "Une application pour apprendre la langue Baoulé."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.5.0
  firebase_auth: ^6.2.0
  cloud_firestore: ^6.1.3
  provider: ^6.1.5+1
  google_fonts: ^8.1.0
  go_router: ^17.2.3
  intl: ^0.20.2
  firebase_ai: ^3.9.0
  flutter_spinkit: ^5.2.2
  percent_indicator: ^4.2.5
  audioplayers: ^6.6.0
  flutter_webrtc: ^1.4.1
  uuid: ^4.5.3
  google_generative_ai: ^0.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
